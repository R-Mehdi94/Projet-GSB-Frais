<form method="post" action="{{ path('app_fichefrais_delete', {'mois': fichefrai.mois}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fichefrai.mois) }}">
    <button class="btn">Delete</button>
</form>
